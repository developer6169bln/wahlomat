# Railway PostgreSQL
# Füge einen PostgreSQL-Service in Railway hinzu.
# DATABASE_URL wird automatisch vom PostgreSQL-Service bereitgestellt.

# Nur für lokale Entwicklung (Railway CLI: railway run npm start):
# DATABASE_URL=postgresql://user:pass@host:5432/railway
